source venv_linux/bin/activate

venv_win\Scripts\activate

git add .

git commit -m "הסבר קצר על השינוי"

git push (בלי תוספות, הוא כבר ידע לאן לשלוח).

אופציה 2: "תדרוס את מה שיש לי פה, ה-Git תמיד צודק"
אם אתה יודע שמה שיש ב-Git הוא הגרסה הכי עדכנית ומה ששינית בטעות במחשב הזה לא חשוב:

Bash

git checkout SVapp/monitor_config.py
git pull

git pull (אפשר לפני שנכנסים ל-venv, כדי לוודא שהקבצים מעודכנים).

venv\Scripts\activate (נכנסים לבועה של פאייתון).

python server_app.py (מפעילים את השרת).